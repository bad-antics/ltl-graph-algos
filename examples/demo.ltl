// examples/demo.ltl — runnable demo for ltl_graph_algos

import std::io
use ltl_graph_algos as lib

fn main() {
    io::println("ltl_graph_algos demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}